const API_BASE_URL = 'https://zed-store.com/api/site/v1';

const API_ENDPOINTS = {
  REGISTER: '/auth/register',
  LOGIN: '/auth/login',
  USER_PROFILE: '/user/profile',
  // Add more endpoints as needed
};

export { API_BASE_URL, API_ENDPOINTS };